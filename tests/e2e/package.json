{
  "name": "secman-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for secman frontend",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:chrome": "npx playwright test --project=chrome",
    "test:edge": "npx playwright test --project=msedge",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
