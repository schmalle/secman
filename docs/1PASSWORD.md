# 1Password — DEPRECATED

This project no longer uses 1Password CLI (`op` / `op run`). The canonical secret-resolution tool is **`pass-cli`** (Proton Pass), per `CLAUDE.md`.

→ **See [PASS_CLI.md](./PASS_CLI.md).**

Any remaining `op://…` references in this repo's scripts should be migrated to `pass-cli` lookups.
